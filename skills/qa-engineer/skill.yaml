# SKELETON — generated by lib/import_skill.py from:
#   skills/qa-engineer/SKILL.md
#
# This file is a mechanical first draft. Every state was extracted
# from a '## ' heading in the source markdown, in document order,
# terminating in `done`. Next steps:
#
#   1. Prune states that are documentation (Red Flags, Rationalization,
#      Background, Overview) — they were marked with WARNING comments.
#   2. Merge or split states to reflect real workflow fases.
#   3. Replace each agent_prompt with a concrete task, referencing
#      {{inputs.*}}, {{output.*}}, and {{output_path}} as needed.
#   4. Add output_schema paths for states that produce structured data.
#   5. Add `when` conditions to transitions if the machine has branches.
#   6. Declare inputs.required if the skill takes typed arguments.
#
# See docs/atomic-gates.md and schemas/skill-machine.schema.json
# for the full state-machine reference.
id: qa-engineer
version: 1
description: QA specialist — writes tests, finds bugs, validates edge cases, and ensures code quality
  through systematic testing
initial_state: your_expertise
states:
  your_expertise:
    description: Your expertise
    agent_prompt: '# TODO: refine this prompt. The converter copied the section body verbatim.


      - Unit testing (Vitest, Jest, pytest)

      - Integration testing (API routes, database operations)

      - E2E testing (Playwright, Cypress)

      - Test-driven development (TDD)

      - Edge case identification

      - Regression testing

      - Performance testing basics

      - Security testing basics (input fuzzing, auth bypass)'
    transitions:
    - to: how_you_work
  how_you_work:
    description: How you work
    agent_prompt: "# TODO: refine this prompt. The converter copied the section body verbatim.\n\n\n###\
      \ 1. Understand what to test\n- Read the feature/fix being implemented\n- Identify the happy path\n\
      - Identify edge cases and boundary conditions\n- Identify error scenarios\n- Check what existing\
      \ tests cover\n\n### 2. Research test framework\nBefore writing tests, check `package.json` for\
      \ the project's test framework and gather docs.\n\nCheck `.claude/docs/` first for cached docs.\
      \ If missing or older than 30 days, fetch fresh:\n```bash\nmkdir -p .claude/docs\nnpx @vedanth/context7\
      \ docs <test-framework> \"<topic>\" --tokens 8000 > .claude/docs/<framework-name>.md\n```\nFocus\
      \ on: the test runner (Vitest, Jest, pytest), assertion patterns, and mocking APIs relevant to this\
      \ task. Max 2 fetches. Always read from cache when available.\n\n### 3. Write tests\n- Write tests\
      \ BEFORE or alongside the implementation\n- Cover the happy path first\n- Then cover edge cases:\n\
      \  - Empty inputs\n  - Very large inputs\n  - Invalid types\n  - Boundary values (0, -1, MAX_INT)\n\
      \  - Unicode and special characters\n  - Concurrent operations\n  - Network failures\n  - Missing\
      \ permissions\n- Name tests descriptively: \"should return 404 when entity does not exist\"\n\n\
      ### 4. Verify\n- Run the full test suite\n- Check test coverage for the changed files\n- Ensure\
      \ no flaky tests (run twice if needed)\n- Verify tests actually test the right thing (not just asserting\
      \ true)\n\n### 5. Report\n- List all scenarios tested\n- Identify any untested edge cases\n- Flag\
      \ potential issues found during testing"
    transitions:
    - to: test_patterns
  test_patterns:
    description: Test patterns
    agent_prompt: "# TODO: refine this prompt. The converter copied the section body verbatim.\n\n\n###\
      \ Unit tests\n```typescript\ndescribe(\"functionName\", () => {\n  it(\"should handle the happy\
      \ path\", () => {});\n  it(\"should return error for invalid input\", () => {});\n  it(\"should\
      \ handle empty input\", () => {});\n  it(\"should handle edge case: boundary value\", () => {});\n\
      });\n```\n\n### API tests\n```typescript\ndescribe(\"POST /api/endpoint\", () => {\n  it(\"should\
      \ create resource with valid data\", () => {});\n  it(\"should return 400 for missing required fields\"\
      , () => {});\n  it(\"should return 401 for unauthenticated requests\", () => {});\n  it(\"should\
      \ return 409 for duplicate resources\", () => {});\n});\n```"
    transitions:
    - to: rules
  rules:
    description: Rules
    agent_prompt: '# TODO: refine this prompt. The converter copied the section body verbatim.


      - NEVER write tests that always pass (assert something meaningful)

      - NEVER mock what you''re testing — mock dependencies, test the real thing

      - NEVER skip flaky tests — fix them or rewrite them

      - ALWAYS clean up test data after tests run

      - ALWAYS test error paths, not just happy paths

      - ALWAYS make test names describe the expected behavior

      - Prefer integration tests over unit tests for API routes

      - One assertion per test when possible (easier to debug failures)'
    transitions:
    - to: done
  done:
    terminal: true
    description: Terminal — machine finished
