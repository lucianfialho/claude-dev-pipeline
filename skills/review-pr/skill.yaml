# SKELETON — generated by lib/import_skill.py from:
#   skills/review-pr/SKILL.md
#
# This file is a mechanical first draft. Every state was extracted
# from a '## ' heading in the source markdown, in document order,
# terminating in `done`. Next steps:
#
#   1. Prune states that are documentation (Red Flags, Rationalization,
#      Background, Overview) — they were marked with WARNING comments.
#   2. Merge or split states to reflect real workflow fases.
#   3. Replace each agent_prompt with a concrete task, referencing
#      {{inputs.*}}, {{output.*}}, and {{output_path}} as needed.
#   4. Add output_schema paths for states that produce structured data.
#   5. Add `when` conditions to transitions if the machine has branches.
#   6. Declare inputs.required if the skill takes typed arguments.
#
# See docs/atomic-gates.md and schemas/skill-machine.schema.json
# for the full state-machine reference.
id: review-pr
version: 1
description: Review a PR with specialist skills — frontend, backend, security, UX, or all
initial_state: usage
states:
  usage:
    description: Usage
    agent_prompt: '# TODO: refine this prompt. The converter copied the section body verbatim.



      ```

      /review-pr frontend     — React/Next.js, components, a11y, performance

      /review-pr backend      — API design, security, DB patterns, error handling

      /review-pr security     — Security-focused checklist (OWASP, secrets, auth)

      /review-pr ux           — UX heuristics, accessibility, visual hierarchy

      /review-pr all          — Run all applicable specialists in parallel (same as /batch-review)

      ```


      If no argument is provided, defaults to `all` which runs specialists **in parallel** using subagents
      (delegates to `/batch-review`).'
    transitions:
    - to: process
  process:
    description: Process
    agent_prompt: '# TODO: refine this prompt. The converter copied the section body verbatim.



      ### 1. Get the PR diff


      Detect the current PR from the branch:

      ```bash

      gh pr view --json number,title,body,files

      gh pr diff

      ```


      If not on a PR branch, ask the user for a PR number.


      ### 2. Dispatch to specialist


      Based on the argument, load the corresponding specialist skill and provide it with:

      - The full PR diff

      - The PR description for context

      - The review output format (below)


      | Argument | Specialist Skill | Focus Area |

      |----------|-----------------|------------|

      | `frontend` | `/frontend-dev` | React/Next.js patterns, components, a11y, responsiveness, performance,
      state management |

      | `backend` | `/backend-dev` | API design, database, auth, error handling, input validation, security
      |

      | `security` | `/code-reviewer` | Security-only: injection, XSS, CSRF, secrets, auth bypass, dependencies
      |

      | `ux` | `/ux-designer` | Nielsen''s heuristics, accessibility, visual hierarchy, interaction design,
      empty states |

      | `all` | All of the above | Run each sequentially, combine output |


      ### 3. Review output format


      Every specialist MUST output findings in this consistent format:


      ```'
    transitions:
    - to: specialist_name_review
  specialist_name_review:
    description: <Specialist Name> Review
    agent_prompt: "# TODO: refine this prompt. The converter copied the section body verbatim.\n\n\n###\
      \ Summary\n<1-2 sentence overview of the review>\n\n### Findings\n\n#### \U0001F534 Critical\nIssues\
      \ that must be fixed before merge.\n\n- **`file.tsx:42`** — <description>\n  **Fix:** <concrete\
      \ suggestion>\n\n#### \U0001F7E1 Warning\nIssues that should be fixed but aren't blocking.\n\n-\
      \ **`file.ts:15`** — <description>\n  **Fix:** <concrete suggestion>\n\n#### \U0001F4A1 Suggestion\n\
      Improvements that would make the code better.\n\n- **`file.ts:88`** — <description>\n  **Fix:**\
      \ <concrete suggestion>\n\n### Verdict\n<APPROVE | REQUEST_CHANGES | COMMENT>\n<1 sentence justification>\n\
      ```\n\n### 4. For `review all`\n\nDelegate to the `/batch-review` skill, which runs specialists\
      \ **in parallel** using subagents:\n\n1. Analyze the PR diff to categorize changed files\n2. Select\
      \ applicable specialists based on file types\n3. Dispatch up to 3 subagents in parallel\n4. Produce\
      \ a unified review with combined verdict table\n\nThis is equivalent to running `/batch-review`\
      \ directly."
    transitions:
    - to: rules
  rules:
    description: Rules
    agent_prompt: '# TODO: refine this prompt. The converter copied the section body verbatim.



      - ALWAYS fetch the actual PR diff — never review from memory

      - ALWAYS use the consistent output format above

      - NEVER auto-approve — always provide at least the verdict

      - If a specialist finds no issues, say so explicitly: "No issues found in <area>"

      - For `review all`, include a final combined verdict based on the worst finding across all specialists

      - Be thorough but concise — explain WHY something is an issue

      - Provide concrete fix suggestions, not just complaints'
    transitions:
    - to: done
  done:
    terminal: true
    description: Terminal — machine finished
