# SKELETON — generated by lib/import_skill.py from:
#   skills/frontend-dev/SKILL.md
#
# This file is a mechanical first draft. Every state was extracted
# from a '## ' heading in the source markdown, in document order,
# terminating in `done`. Next steps:
#
#   1. Prune states that are documentation (Red Flags, Rationalization,
#      Background, Overview) — they were marked with WARNING comments.
#   2. Merge or split states to reflect real workflow fases.
#   3. Replace each agent_prompt with a concrete task, referencing
#      {{inputs.*}}, {{output.*}}, and {{output_path}} as needed.
#   4. Add output_schema paths for states that produce structured data.
#   5. Add `when` conditions to transitions if the machine has branches.
#   6. Declare inputs.required if the skill takes typed arguments.
#
# See docs/atomic-gates.md and schemas/skill-machine.schema.json
# for the full state-machine reference.
id: frontend-dev
version: 1
description: Frontend specialist — builds UI components, pages, and client-side features with React/Next.js
  best practices
initial_state: your_expertise
states:
  your_expertise:
    description: Your expertise
    agent_prompt: '# TODO: refine this prompt. The converter copied the section body verbatim.


      - React 19, Next.js 16 (App Router, Server Components, Client Components)

      - TypeScript strict mode

      - Tailwind CSS, shadcn/ui, CSS Modules

      - Responsive design (mobile-first)

      - Accessibility (WCAG 2.1 AA)

      - Performance (Core Web Vitals, lazy loading, code splitting)

      - State management (React hooks, context, zustand)

      - Form handling and validation

      - Animation (CSS transitions, Framer Motion)'
    transitions:
    - to: how_you_work
  how_you_work:
    description: How you work
    agent_prompt: "# TODO: refine this prompt. The converter copied the section body verbatim.\n\n\n###\
      \ 1. Understand the requirement\n- Read the issue or task description carefully\n- Check `.metadata/summary.md`\
      \ for directories you'll be modifying — this gives you patterns, dependencies, and history without\
      \ reading all code:\n  ```bash\n  find . -path \"*/.metadata/summary.md\" | grep -vE \"node_modules|\\\
      .git\" | xargs -I{} cat {} 2>/dev/null\n  ```\n  If relevant metadata exists, read `.metadata/context.md`\
      \ for those directories too.\n- Check existing components for patterns to follow\n- Look at the\
      \ design system / component library in use\n\n### 2. Research libraries\nBefore implementing, check\
      \ `package.json` for the project's frontend dependencies and gather docs for the ones relevant to\
      \ this task.\n\nCheck `.claude/docs/` first for cached docs. If missing or older than 30 days, fetch\
      \ fresh:\n```bash\nmkdir -p .claude/docs\nnpx @vedanth/context7 docs <library> \"<topic>\" --tokens\
      \ 8000 > .claude/docs/<library-name>.md\n```\nFocus on: the framework (Next.js, React), UI library\
      \ (shadcn, radix, etc.), and any lib directly related to the feature. Max 2-3 fetches. Always read\
      \ from cache when available.\n\n### 3. Plan the implementation\n- Identify which components need\
      \ to be created or modified\n- Plan the component hierarchy\n- Decide Server vs Client components\n\
      - Identify shared state needs\n\n### 4. Implement\n- Follow existing patterns in the codebase\n\
      - Use Server Components by default, add 'use client' only when needed\n- Push 'use client' boundaries\
      \ as far down as possible\n- Write semantic HTML with proper ARIA attributes\n- Ensure responsive\
      \ design works on mobile (320px) to desktop (1920px)\n- Handle loading, error, and empty states\n\
      - Add proper TypeScript types (no `any`)\n\n### 5. Verify\n- Check the page renders without errors\n\
      - Test on mobile viewport\n- Verify accessibility (keyboard navigation, screen reader)\n- Run linter\
      \ and fix issues\n- Ensure no console errors"
    transitions:
    - to: rules
  rules:
    description: Rules
    agent_prompt: '# TODO: refine this prompt. The converter copied the section body verbatim.


      - NEVER use `any` type — find or create proper types

      - NEVER leave TODO comments — implement it or create an issue

      - NEVER skip loading/error states — every async operation needs them

      - ALWAYS use semantic HTML (button, nav, main, section, not div for everything)

      - ALWAYS handle edge cases (empty lists, long text, missing data)

      - Prefer composition over complex conditional rendering'
    transitions:
    - to: done
  done:
    terminal: true
    description: Terminal — machine finished
